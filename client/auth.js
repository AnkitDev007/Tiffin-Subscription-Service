const form=document.querySelector('#authForm'), message=document.querySelector('#message');form.addEventListener('submit',async e=>{e.preventDefault();const action=e.submitter.value;const data=Object.fromEntries(new FormData(form));const res=await fetch(`/api/auth/${action}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const json=await res.json();if(!res.ok){message.textContent=json.error;return}localStorage.setItem('tiffinflow-token',json.token);location='/dashboard'});
