const form=document.querySelector('#authForm'),message=document.querySelector('#message'),nameField=document.querySelector('#nameField'),title=document.querySelector('#authTitle'),hint=document.querySelector('#authHint'),submit=document.querySelector('#submitAuth'),switchMode=document.querySelector('#switchMode'),switchText=document.querySelector('#switchText');let mode='login';function render(){const register=mode==='register';nameField.hidden=!register;nameField.style.display=register?'grid':'none';nameField.querySelector('input').required=register;title.textContent=register?'Create your owner account':'Welcome back';hint.innerHTML=register?'Create an account to start managing subscriptions.':'Use <b>owner@tiffinflow.test</b> / <b>Demo@123</b> to explore the demo.';submit.value=register?'register':'login';submit.textContent=register?'Create account':'Sign in';switchText.textContent=register?'Already have an account?':'New to TiffinFlow?';switchMode.textContent=register?'Sign in instead':'Create an account';message.textContent=''}switchMode.onclick=()=>{mode=mode==='login'?'register':'login';render()};form.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));if(mode==='login')delete data.name;const res=await fetch('/api/auth/'+mode,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const json=await res.json();if(!res.ok){message.textContent=json.error;return}localStorage.setItem('tiffinflow-token',json.token);location='/dashboard'});render();
